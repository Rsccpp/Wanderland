<% layout('/layouts/boilerplate.ejs') %>

<div class="row mt-3">
  <div class="alert alert-danger col-6 offset-3" id="flash-msg" role="alert">
    <%= message %>
  </div>
</div>
<!-- 
<script>
  setTimeout(() => {
    const flash = document.getElementById("flash-msg");
    if(flash){
      flash.style.opacity = "0";
      setTimeout(() => flash.remove(), 300);
    }
  }, 3000)
</script> -->
